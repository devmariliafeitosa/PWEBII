# ⚙️ CarAds API - Backend

API Fake desenvolvida para suportar a aplicação de anúncios automotivos. Utiliza `json-server` para simular um banco de dados persistente em um arquivo JSON.

## 📋 Requisitos da Prática
- Estrutura de dados em `db/db.json` atualizada para incluir: `contato` e `imagemUrl`.

## 🚦 Endpoints
- `GET /anuncios`: Lista todos os veículos.
- `POST /anuncios`: Adiciona um novo veículo.
- `GET /anuncios/:id`: Busca detalhes de um veículo.

## 🚀 Como rodar
1. `npm install`
2. `npm start` (Rodando na porta 3000)